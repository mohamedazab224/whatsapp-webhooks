# 🚀 البداية السريعة - WhatsApp Hub

## في 5 دقائق فقط

### 1️⃣ التثبيت

```bash
npm install
```

### 2️⃣ التكوين

الملف `.env.local` موجود ومُعد بالمعرفات الصحيحة. لا حاجة لتغيير شيء!

### 3️⃣ التشغيل

```bash
npm run dev
```

### 4️⃣ الاختبار

افتح المتصفح:
- الصفحة الرئيسية: `http://localhost:3000`
- لوحة التحكم: `http://localhost:3000/control`

### 5️⃣ إرسال رسالة تجريبية

1. اذهب إلى: `http://localhost:3000/control`
2. في نموذج "إرسال رسالة":
   - رقم الهاتف: `15557285727`
   - الرسالة: `مرحبا! هذه رسالة تجريبية من WhatsApp Hub`
3. اضغط "إرسال الرسالة"
4. تحقق من الـ console للتأكد

---

## إعداد Webhook (اختياري للتطوير)

### باستخدام ngrok:

```bash
# في terminal جديد
ngrok http 3000
```

انسخ الرابط (مثل: `https://abc123.ngrok.io`) واذهب إلى:
[Meta Webhook Settings](https://developers.facebook.com/apps/724370950034089/whatsapp-business/wa-settings/)

- **Callback URL**: `https://abc123.ngrok.io/api/webhook`
- **Verify Token**: `uberfix_webhook_secure_2024_token`

---

## الصفحات المتاحة

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | صفحة الترحيب |
| لوحة التحكم الكاملة | `/control` | إرسال واستقبال الرسائل |
| لوحة المراجعة | `/dashboard` | عرض الرسائل والملاحظات |

---

## استكشاف سريع للأخطاء

### الرسالة لا ترسل؟
- تأكد من تشغيل `npm run dev`
- استخدم رقم الاختبار: `15557285727` أو `+15557285727`
- افحص console logs

### Webhook لا يعمل؟
- تأكد من تشغيل ngrok
- تأكد من إضافة `/api/webhook` في نهاية الرابط
- تأكد من تطابق Verify Token

---

✅ **جاهز للعمل!** - للمزيد من التفاصيل راجع `PRODUCTION_SETUP.md`
